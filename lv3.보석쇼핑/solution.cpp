#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<string> gems) {
    vector<int> answer;
    vector<string> gems_kind;
    gems_kind.push_back(gems[0]);
    for(int i=1;i<gems.size();i++){
        if(gem)
    }
    return answer;
}